# Production Configuration (Supabase PostgreSQL)

# Database Configuration (Supabase) - nekota-serverと同一DB使用
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.xsglqqywodyqhzktkygq.supabase.co:5432/postgres?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:y8&LkP#3bT9uQx!r}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# PostgreSQL specific settings
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Disable H2 Console in production
spring.h2.console.enabled=false
