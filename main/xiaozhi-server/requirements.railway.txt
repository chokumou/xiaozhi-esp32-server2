# Minimal requirements for Railway deployment (exclude heavy ML/CUDA packages)
fastapi==0.104.1
starlette>=0.27.0
uvicorn[standard]==0.24.0
pydantic>=2.7.3,<3.0.0
python-multipart>=0.0.9
requests==2.32.3
loguru==0.7.3
aiohttp==3.9.3
aioconsole==0.8.1
websockets==14.2
numpy==1.26.4
# HTTP client used by manage_api_client
httpx==0.27.2
# Opus library used by util.py
opuslib_next==1.1.2
# Audio utility
pydub==0.25.1
# JWT lib
PyJWT==2.8.0
# Cryptography used by core.utils.auth
cryptography>=40.0.0
webrtcvad==2.0.10
PyYAML>=6.0
aiohttp-cors==0.7.0
# Template lib used by some handlers
jinja2==3.1.6
# HTML parser used by some utilities
bs4==0.0.2
# small markdown helper used by codebase
markitdown==0.1.1
# mcp removed to avoid anyio version conflict; server MCP support will be optional at runtime
# Add any other lightweight libs required by server runtime here
cnlunar==0.2.0
# OpenAI API client (required for ASR and LLM)
openai>=1.0.0


